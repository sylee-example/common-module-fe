# 서비스 단위 분석 — 짧은 프롬프트 (psm* 기준)

> 전수 분석(LEGACY_ANALYSIS_GUIDE.md) 대신 **서비스 1개씩** 잘라서 본다.
> 공통 영역은 위에서 찾지 않고 **서비스에서 역으로 추적**한다.
> 프롬프트는 전부 10줄 이내. 모델은 판단만 한다.

---

## 1. 스크립트 1번 실행 (여기서 90%가 끝난다)

```powershell
.\scripts\analyze.ps1 -Root "D:\legacy" -ServicePrefix "psm"
```

나오는 것:

| 파일 | 내용 |
|---|---|
| `10_services.csv` | psm* 폴더마다 — **실행형태**(WAR 독립배포 / 라이브러리 / 배치 / 화면만), web.xml, URL패턴, Java·JSP·Mapper 수, 외부의존 파일 수 |
| `10_service-deps.csv` | 서비스 → 폴더 밖에서 끌어다 쓰는 파일 전수 (**전이 추적 포함**) |
| `10_common-shared.csv` | 그 역인덱스. 공통파일 → 쓰는 서비스 목록 + 판정 |

`10_common-shared.csv` 의 판정이 핵심:

```
사용 서비스 3개 이상 → 진짜 공통. platform-core 후보
사용 서비스 2개      → 복제할지 공통화할지 판단 필요
사용 서비스 1개      → 공통 아님. 그 서비스로 흡수 (= 공통모듈에서 빼도 되는 것)
```

> 레거시 공통 모듈의 상당수가 "1개 서비스만 쓰는 것"으로 나온다. **그게 컨버전 범위를 가장 크게 줄인다.**

---

## 2. 서비스 하나 고르기

`10_services.csv` 를 열고 **`실행형태 = WAR 독립 배포` 이면서 파일 수가 가장 적은 것**부터 시작한다.
그 서비스 하나를 끝까지 컨버전해서 레퍼런스를 만든다. 그 다음부터 속도가 붙는다.

---

## 3. 프롬프트

> **분석 단계에서 쓰는 프롬프트는 P1 하나뿐이다.**
> P2~P4 는 컨버전을 실제로 시작할 때 쓴다. 지금 읽지 않아도 된다.

### P1. 공통 의존 분류 ← 분석 단계는 이것만 하면 끝

```text
아래는 이 서비스가 폴더 밖에서 끌어다 쓰는 파일 목록이다.
각 행을 A~E 중 하나로 분류하라. 다른 말 금지.

A=유틸(문자열/날짜/숫자/암호화)  B=DB공통  C=인증/세션/권한
D=화면공통(태그/JSP조각)  E=프레임워크코어(요청 디스패치/공통 Action)

출력(행마다 한 줄): <파일경로>|<A~E>
예시: common/util/StringUtil.java|A

[목록] {{10_service-deps.csv 에서 이 서비스 행 30개씩}}
```

> **E가 하나라도 나오면** 그 서비스는 단독 마이그레이션 불가.
> 프레임워크 코어를 먼저 Spring MVC로 대체해야 한다.

---

## 3-1. 컨버전 시작할 때 쓰는 프롬프트 (지금은 건너뛸 것)

### P2. 이 서비스가 뭐 하는 서비스인지

```text
아래는 레거시 서비스 하나의 파일 목록과 URL 패턴이다.
이 서비스가 무슨 업무를 하는지 3줄 이내로 쓰고, 화면 목록을 나열하라.
모르는 건 UNKNOWN. 추측 금지.

출력:
업무: <3줄 이내>
화면: <JSP경로>|<화면명>   (한 줄에 하나)

[서비스] {{10_services.csv 해당 행}}
[JSP 목록] {{03_jsp-inventory.csv 에서 이 서비스 경로로 필터한 행}}
```

### P3. 화면 → API 매핑

```text
아래 JSP 하나를 보고 필요한 API를 뽑아라. 없는 건 만들지 마라.

출력(한 줄에 하나):
<HTTP메서드>|<URL>|<파라미터 콤마구분>|<응답형태: json 또는 화면>
예시: GET|/psm/user/list.do|searchType,keyword|화면

[JSP] {{원본 JSP. 300줄 넘으면 <form>과 $.ajax 부분만}}
```

### P4. 화면명 + React 컴포넌트명

```text
아래 JSP 경로마다 한국어 화면명(10자 이내)과 React 컴포넌트명을 정하라.
컴포넌트명은 PascalCase, 끝은 Page/Modal/Form/Grid 중 하나.
경로만으로 모르겠으면 화면명에 UNKNOWN. 지어내지 마라.

출력: <경로>|<화면명>|<컴포넌트명>
예시: /psm/user/userList.jsp|사용자 목록|UserListPage

[목록] {{경로 30개}}
```

---

## 4. 모델에게 안 시키는 것

전부 `analyze.ps1` 이 이미 만들어 놨다. 프롬프트를 쓰지 마라.

```
파일 수 / LOC / 인코딩          → 00_files.csv
서비스 실행 형태 / URL 패턴      → 10_services.csv
공통 의존 추적 (전이 포함)       → 10_service-deps.csv
공통/전용 판정                   → 10_common-shared.csv
SQL statement 목록 / 참조 테이블 → 05_mapper-statements.csv
Oracle 종속 문법                 → 05_oracle-dependency.csv
죽은 코드                        → 09_dead-*.csv
라이브러리 호환성                → 00_dependencies.csv
```

---

## 5. 다음 단계

P1~P4 결과가 나오면 **CONVERSION_GUIDE.md 의 19장 티켓**을 채워서 컨버전으로 넘어간다.
서비스 1개 = 티켓 N개. 티켓 1개 = 파일 1개씩.

---

## 6. 전수 분석(LEGACY_ANALYSIS_GUIDE.md)은 언제 쓰나

| 상황 | 문서 |
|---|---|
| 서비스 몇 개만 먼저 옮긴다 | **이 문서** |
| 전체 이행 계획·공수 산정·웨이브 편성이 필요하다 | LEGACY_ANALYSIS_GUIDE.md |
| 컨버전 실제 작업 | CONVERSION_GUIDE.md |

평소에는 이 문서만 쓰면 된다. 전수 분석은 경영보고·예산 산정할 때 한 번 돌린다.
